server:
    port: 10344
 
spring:
    profiles: dev

    datasource:
        url: jdbc:h2:mem:semantheque-sid-test

    jpa:
        database-platform: org.hibernate.dialect.H2Dialect
        database: H2
